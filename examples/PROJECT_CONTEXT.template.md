# Project Context

Drop this (renamed to `PROJECT_CONTEXT.md`) into your project's `.agents/`
folder. Every agent reads it at spawn.

## Project name
<name>

## What it is
<2-3 sentences on purpose>

## Key packages / components
- `packages/foo` — does X
- `packages/bar` — does Y

## Conventions
- Testing: <what matters, e.g. "integration tests hit a real DB">
- Code style: <reference, e.g. "follow existing patterns in this file">
- Branches: <convention>

## Environments
- Local: <how to run>
- Emulator / dev: <how to hit it>
- Production: <hands-off? read-only?>

## Current focus
<what this session is actually for>
